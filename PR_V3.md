Darwin now starts directly on current Hyperliquid public data with paper execution. The synthetic-data selector and API session entry are removed. A desktop shortcut starts the local Factory and engineering worker.

Based on `darwin-v0.11-factory-evolution` (PR #1); this PR contains the V3 increment.

- Autonomous research: ATLAS/CURIE/EVOLVE/JUDGE critique/MNEMOSYNE use schema-validated OpenRouter free responses, shared quota accounting, bounded parameter mutations and persisted parent/descendant comparisons. LLM calls run outside the event loop; paper accounts remain flat during analysis between windows, while the feed and UI continue updating.
- Engineering: Codex CLI reuses the existing ChatGPT login to propose a constrained patch in an isolated worktree. Independent contracts, backend regression, TypeScript and build run before human review. No generated code is integrated automatically.
- Factory: real provider status, holding durations, paper trades, research cycle status and code proposal evidence. Windows launcher and desktop shortcut included.

Validation: 81 backend tests and frontend build passed locally. Real Codex run `c8e7f86a3d4d4c0ea6200ecb979d75ce` reached READY_FOR_REVIEW, with candidate `89bb709ecbf34791fa3b01c8a023eb2cfedf70ac`; all regression/build checks passed after the expected defect-reproduction failure. Real OpenRouter responses were verified. Runtime reports healthy Hyperliquid public data and rejects simulation sessions. GitHub checks are reported separately below this description.

Limits: the controlled code demo fixes a display helper, not trading formulas. Strategy evolution adjusts existing bounded genes; it does not invent arbitrary executable strategies. No profitability or overnight improvement is established. Funding and real market impact are not modeled; accounts are independent, not a delta/gamma-neutral portfolio. Real trading remains disabled; public paper requires no wallet. No deployment or merge is included.

Branding: warm copper/ivory origami identity, Swarm flock in the sidebar/favicon and Darwin fox inside Factory and on the desktop shortcut. `Fusionner-Darwin-PR2.cmd` wraps the installed PR Completion helper, checks the exact PR/head/base and CI, then requires explicit exact-SHA confirmation; no merge is performed by installation.
